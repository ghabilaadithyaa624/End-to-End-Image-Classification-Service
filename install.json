{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "venv": "env",
        "message": [
          "python -m pip install --upgrade pip",
          "pip install -r requirements.txt"
        ]
      }
    },
    {
      "method": "notify",
      "params": {
        "html": "<b>Installation Complete!</b> Click <b>Start</b> to launch the Gradio app."
      }
    }
  ]
}
