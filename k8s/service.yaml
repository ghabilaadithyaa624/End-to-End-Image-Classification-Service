apiVersion: v1
kind: Service
metadata:
  name: image-classification-service
  labels:
    app: image-classification-service
spec:
  type: ClusterIP
  selector:
    app: image-classification-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
